package introduce

/**
 * @package
 * @createTime 2023/6/4 12:57
 * @author SeedList
 * @Description
 * scala的集合有三大类：序列Seq、集Set、映射Map，所有的集合都扩展自Iterable特质
 * 对于几乎所有的集合类，scala都同时提供了可变和不可变的版本，分别位于以下两个包
 * 不可变集合：scala.collection.immutable
 * 可变集合：scala.collection.mutable
 * 不可变集合，就是指该集合对象不可修改，每次修改就会返回一个新的对象，而不会对原对象进行修改，类似于java中的String对象
 * 可变集合，就是这个集合可以直接对原对象进行修改，而不会返回新的对象。类似于java中的StringBuilder对象
 */
object basisIntroduce {
}
